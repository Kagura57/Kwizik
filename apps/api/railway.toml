[build]
builder = "NIXPACKS"
buildCommand = "if [ -d apps/api ]; then cd apps/api; fi && bun install"

[deploy]
startCommand = "if [ -d apps/api ]; then cd apps/api; fi && bun run start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
